import { useSpring } from 'react-spring'

export const welcome = useSpring({
    delay:200,
    opacity:1,
    from:{
        opacity:0
    }
    
})
export const to = useSpring({
    delay:400,
    opacity:1,
    from:{
        opacity:0
    }
    
})

export const viridis = useSpring({
    delay:600,
    opacity:1,
    from:{
        opacity:0
    }
    
})

export const venture = useSpring({
    delay:800,
    opacity:1,
    from:{
        opacity:0
    }
    
})